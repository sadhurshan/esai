# Deep Spec Pack — Elements Supply AI
These files expand `/docs` into per-module specs Copilot can consume.
Keep **this folder OPEN in VS Code** while prompting.

Structure per spec:
- Scope & Goals
- Data Model (fields, enums, indexes)
- API Surface (endpoints, payloads, validation)
- UI States (pages, components, empty/skeleton)
- Workflows & Rules
- Notifications
- Permissions
- Tests & Acceptance
